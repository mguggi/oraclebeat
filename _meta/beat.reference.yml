########################## Metricbeat Configuration ###########################

# This file is a full configuration example documenting all non-deprecated
# options in comments. For a shorter configuration example, that contains only
# the most common options, please see metricbeat.yml in the same directory.
#
# You can find the full configuration reference here:
# https://www.elastic.co/guide/en/beats/metricbeat/index.html

#============================  Config Reloading ===============================

# Config reloading allows to dynamically load modules. Each file which is
# monitored must contain one or multiple modules as a list.
metricbeat.config.modules:

  # Glob pattern for configuration reloading
  path: ${path.config}/conf.d/*.yml

  # Period on which files under path should be checked for changes
  reload.period: 10s

  # Set to true to enable config reloading
  reload.enabled: false

# Maximum amount of time to randomly delay the start of a metricset. Use 0 to
# disable startup delay.
metricbeat.max_start_delay: 10s

#==========================  Modules configuration ============================
oraclebeat.modules:

#---------------------------- Oracle Module Module ---------------------------
- module: oracle
  metricsets: ["sqlstats"]
  enabled: true
  period: 10s

  # List of Oracle connection strings
  hosts: ["oracle://system:oraclebeat@10.1.87.87/ora12"]

  # Timeout after which time a metric-set should return an error
  # Timeout is by default defined as period, as a fetch of a metric-set
  # should never take longer then period, as otherwise calls can pile up.
  #timeout: 1s


